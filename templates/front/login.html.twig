<!DOCTYPE html>
<html lang="fr">

  <head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="TemplateMo">
    <link href="https://fonts.googleapis.com/css?family=Poppins:100,200,300,400,500,600,700,800,900&display=swap" rel="stylesheet">

    <title>Finovate - Connexion</title>

    <base href="{{ asset('front/') }}">

    <link href="vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <link rel="stylesheet" href="assets/css/fontawesome.css">
    <link rel="stylesheet" href="assets/css/templatemo-finance-business.css">
    <link rel="stylesheet" href="assets/css/owl.css">

    <style>
      .fin-login-card {
        background: #ffffff;
        border-radius: 16px;
        box-shadow: 0 18px 55px rgba(0, 0, 0, 0.12);
        padding: 28px;
        border: 1px solid rgba(26, 95, 58, 0.08);
      }
      .fin-login-title {
        margin-bottom: 14px;
        font-weight: 800;
        color: #1a5f3a;
        letter-spacing: -0.5px;
      }
      .fin-login-subtitle {
        color: #6b7280;
        font-size: 0.95rem;
        margin-bottom: 18px;
      }
      .fin-input {
        height: 48px;
        border-radius: 12px;
        border: 2px solid #e5e7eb;
        padding: 10px 14px;
        transition: border-color .15s ease, box-shadow .15s ease;
        background: #ffffff;
      }
      .fin-input:focus {
        border-color: #237f4e;
        box-shadow: 0 0 0 .2rem rgba(35, 127, 78, .15);
      }
      .fin-actions {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .fin-google-btn {
        width: 100%;
        border-radius: 12px;
        height: 46px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        border: 2px solid #dc2626;
        background: #dc2626;
        color: #ffffff;
        font-weight: 600;
      }
      .fin-google-btn:hover {
        background: #b91c1c;
        border-color: #b91c1c;
        color: #ffffff;
      }
      .fin-github-btn {
        width: 100%;
        border-radius: 12px;
        height: 46px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        border: 2px solid #24292e;
        background: #24292e;
        color: #ffffff;
        font-weight: 600;
      }
      .fin-github-btn:hover {
        background: #1a1e22;
        border-color: #1a1e22;
        color: #ffffff;
      }
      .fin-divider {
        display: flex;
        align-items: center;
        gap: 10px;
        margin: 6px 0;
        color: #94a3b8;
        font-size: 0.9rem;
      }
      .fin-divider:before,
      .fin-divider:after {
        content: "";
        height: 1px;
        flex: 1;
        background: #e5e7eb;
      }
      .fin-links {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 10px;
        margin-top: 10px;
      }
      .fin-links a {
        font-weight: 600;
      }
    </style>

  </head>

  <body>

    <div id="preloader">
        <div class="jumper">
            <div></div>
            <div></div>
            <div></div>
        </div>
    </div>

    

    <header class="">
      <nav class="navbar navbar-expand-lg">
        <div class="container">
          <a class="navbar-brand" href="{{ path('front_home') }}"><h2>Finovate</h2></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
              <li class="nav-item">
                <a class="nav-link" href="{{ path('front_home') }}">Accueil</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('front_about') }}">À propos</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('front_services') }}">Nos services</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('front_contact') }}">Contact</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('front_one_page') }}">Présentation</a>
              </li>
              <li class="nav-item active" id="nav-login">
                <a class="nav-link" href="{{ path('front_login') }}">Connexion</a>
              </li>
              <li class="nav-item" id="nav-dashboard" style="display: none;">
                <a class="nav-link" href="{{ path('user_dashboard') }}">Mon espace</a>
              </li>
              <li class="nav-item" id="nav-logout" style="display: none;">
                <a class="nav-link" href="{{ path('front_logout') }}">Déconnexion</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <div class="page-heading header-text">
      <div class="container">
        <div class="row">
          <div class="col-md-12">
            <h1>Connexion</h1>
            <span>Accédez à votre espace Finovate</span>
          </div>
        </div>
      </div>
    </div>

    <div class="callback-form contact-us">
      <div class="container">
        <div class="row">
          <div class="col-md-12">
            <div class="section-heading">
              <h2>Se <em>connecter</em></h2>
              <span>Entrez vos identifiants pour accéder à vos comptes</span>
            </div>
          </div>
          <div class="col-lg-6 col-md-10 mx-auto">
            <div class="fin-login-card">
              <h4 class="fin-login-title">Bienvenue</h4>
              <div class="fin-login-subtitle">Connectez-vous pour accéder à votre espace.</div>
              <form id="login" action="" method="post" novalidate="novalidate">
                <div class="row">
                  <div class="col-lg-12 col-md-12 col-sm-12 mb-3">
                    <fieldset>
                      <input name="email" type="email" class="form-control fin-input" id="email" placeholder="Adresse e-mail" required="">
                    </fieldset>
                  </div>
                  <div class="col-lg-12 col-md-12 col-sm-12 mb-2">
                    <fieldset>
                      <input name="password" type="password" class="form-control fin-input" id="password" placeholder="Mot de passe" required="">
                    </fieldset>
                  </div>
                  <div class="col-lg-12 col-md-12 col-sm-12 mb-3">
                    <div class="form-check">
                      <input class="form-check-input" type="checkbox" id="remember_me" name="remember_me">
                      <label class="form-check-label" for="remember_me" style="font-weight: 500; color: #374151;">
                        Se souvenir de moi
                      </label>
                    </div>
                  </div>
                  <div class="col-lg-12" style="margin-top: 14px;">
                    <div class="fin-actions">
                      <button type="submit" class="filled-button" style="width: 100%; border-radius: 12px; height: 46px;">Connexion</button>
                      <div class="fin-divider">ou</div>
                      <a href="{{ path('connect_google_start') }}" class="fin-google-btn">
                        <i class="fa fa-google"></i>
                        Se connecter avec Google
                      </a>
                      <a href="{{ path('connect_github_start') }}" class="fin-github-btn">
                        <i class="fa fa-github"></i>
                        Se connecter avec GitHub
                      </a>
                    </div>

                    <div class="fin-links">
                      <a href="{{ path('front_forgot_password') }}">Mot de passe oublié ?</a>
                      <a href="{{ path('front_signup') }}">Créer un compte</a>
                    </div>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>

    <footer>
      <div class="container">
        <div class="row">
          <div class="col-md-3 footer-item">
            <h4>Liens utiles</h4>
            <ul class="menu-list">
              <li><a href="{{ path('front_home') }}">Accueil</a></li>
              <li><a href="{{ path('front_services') }}">Nos services</a></li>
              <li><a href="{{ path('front_about') }}">À propos</a></li>
              <li><a href="{{ path('front_contact') }}">Contact</a></li>
              <li><a href="{{ path('front_one_page') }}">Présentation</a></li>
            </ul>
          </div>
          <div class="col-md-3 footer-item">
            <h4>Informations</h4>
            <ul class="menu-list">
              <li><a href="#">Tarifs</a></li>
              <li><a href="#">Sécurité</a></li>
              <li><a href="#">FAQ</a></li>
              <li><a href="#">Support</a></li>
              <li><a href="#">Politique de confidentialité</a></li>
            </ul>
          </div>
          <div class="col-md-3 footer-item last-item">
            <h4>Nous écrire</h4>
            <div class="contact-form">
              <form id="contact-footer" action="{{ path('front_contact_send') }}" method="post" novalidate="novalidate">
                <input type="hidden" name="_token" value="{{ csrf_token('contact_form') }}">
                <input type="hidden" name="subject" value="Contact depuis le footer (Login)">
                <div class="row">
                  <div class="col-lg-12 col-md-12 col-sm-12">
                    <fieldset>
                      <input name="name" type="text" class="form-control" id="name" placeholder="Nom complet" required="" pattern=".*\D.*" title="Le nom complet ne doit pas être un entier.">
                    </fieldset>
                  </div>
                  <div class="col-lg-12 col-md-12 col-sm-12">
                    <fieldset>
                      <input name="email" type="email" class="form-control" id="email" placeholder="Adresse e-mail" required="">
                    </fieldset>
                  </div>
                  <div class="col-lg-12">
                    <fieldset>
                      <textarea name="message" rows="6" class="form-control" id="message" placeholder="Votre message" required=""></textarea>
                    </fieldset>
                  </div>
                  <div class="col-lg-12">
                    <fieldset>
                      <button type="submit" id="form-submit" class="filled-button">Envoyer</button>
                    </fieldset>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </footer>

    <div class="sub-footer">
      <div class="container">
        <div class="row">
          <div class="col-md-12">
            <p>Copyright &copy; 2026 Finovate.
            
            - Design: <a rel="nofollow noopener" href="https://templatemo.com" target="_blank">TemplateMo</a><br>
            Distribué par: <a rel="nofollow noopener" href="https://themewagon.com" target="_blank">ThemeWagon</a>
          </p>
          </div>
        </div>
      </div>
    </div>

    <script src="vendor/jquery/jquery.min.js"></script>
    <script src="vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

    <script src="assets/js/custom.js"></script>
    <script src="assets/js/owl.js"></script>
    <script src="assets/js/slick.js"></script>
    <script src="assets/js/accordions.js"></script>

    <script>
      (function () {
        var loginItem = document.getElementById('nav-login');
        var dashboardItem = document.getElementById('nav-dashboard');
        var logoutItem = document.getElementById('nav-logout');

        if (loginItem && dashboardItem && logoutItem) {
          var token = null;
          try {
            token = localStorage.getItem('finovate_token');
          } catch (e) {}

          var isAuthed = !!token;
          loginItem.style.display = isAuthed ? 'none' : '';
          dashboardItem.style.display = isAuthed ? '' : 'none';
          logoutItem.style.display = isAuthed ? '' : 'none';
        }

        var form = document.getElementById('login');
        if (!form) return;

        function showLoginError(message) {
          var existing = form.querySelector('.login-validation-alert');
          if (existing) existing.remove();

          var alertDiv = document.createElement('div');
          alertDiv.className = 'alert alert-danger login-validation-alert';
          alertDiv.setAttribute('role', 'alert');
          alertDiv.textContent = message;
          form.prepend(alertDiv);
        }

        form.addEventListener('submit', async function (e) {
          e.preventDefault();

          var email = document.getElementById('email') ? document.getElementById('email').value : '';
          var password = document.getElementById('password') ? document.getElementById('password').value : '';

          email = (email || '').trim();
          password = (password || '').trim();

          if (!email) {
            showLoginError('L\'adresse e-mail est obligatoire.');
            return;
          }

          if (!/^\S+@\S+\.\S+$/.test(email)) {
            showLoginError('Veuillez saisir une adresse e-mail valide.');
            return;
          }

          if (!password) {
            showLoginError('Le mot de passe est obligatoire.');
            return;
          }

          try {
            var res = await fetch('/api/login', {
              method: 'POST',
              headers: {
                'Content-Type': 'application/json'
              },
              body: JSON.stringify({ email: email, password: password })
            });

            var data = await res.json().catch(function () { return {}; });

            if (!res.ok || !data.token) {
              showLoginError(data.message || 'Connexion impossible. Vérifie tes identifiants.');
              return;
            }

            var rememberMe = !!document.getElementById('remember_me') && document.getElementById('remember_me').checked;
            if (rememberMe) {
              localStorage.setItem('finovate_token', data.token);
              var expires = new Date();
              expires.setTime(expires.getTime() + 30 * 24 * 60 * 60 * 1000);
              document.cookie = 'finovate_token=' + data.token + '; path=/; expires=' + expires.toUTCString() + '; SameSite=Lax';
            } else {
              sessionStorage.setItem('finovate_token', data.token);
              document.cookie = 'finovate_token=' + data.token + '; path=/; SameSite=Lax';
            }
            var target = (data && data.redirect_url) ? data.redirect_url : '{{ path('user_dashboard') }}';
            window.location.href = target;
          } catch (err) {
            showLoginError('Erreur réseau. Réessaie.');
          }
        });
      })();
    </script>

  </body>
</html>
